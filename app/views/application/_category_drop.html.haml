= form_tag(controller: :restaurants, action: :dropdown, class: 'form-group') do
  = select_tag 'category', options_for_select(dropdown_options), include_blank: 'Pick a Category', onChange: "selection = $(this).val(); addMarkers();", class: 'form-control'
